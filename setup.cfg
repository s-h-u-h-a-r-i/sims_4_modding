[mypy]
mypy_path = stubs
ignore_missing_imports = True

[pylint.MASTER]
init-hook=
    import os, sys
    for _d in (os.getcwd(), os.path.dirname(os.getcwd())):
        _stubs = os.path.join(_d, "stubs")
        if os.path.isdir(_stubs):
            sys.path.insert(0, _stubs)
            break
